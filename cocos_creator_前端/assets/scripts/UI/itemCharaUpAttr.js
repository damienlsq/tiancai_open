cc.Class({
    extends: cc.Component,

    properties: {
        arrtName: cc.RichText,
        oldData: cc.Label,
        newData: cc.Label,
    },

    // use this for initialization
    onLoad: function () {

    },

    // called every frame, uncomment this function to activate update callback
    // update: function (dt) {

    // },
});